[project]
name = "office-agent"
version = "0.1.0"
description = "MCP server / CLI command to interact with Office documents (docx, xslx, pptx)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
